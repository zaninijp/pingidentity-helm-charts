apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Customer360.fullname" . }}-test-connection"
  labels:
    {{- include "Customer360.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Customer360.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
